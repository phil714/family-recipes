import type { Meta, StoryObj } from '@storybook/react'

import CMSLayout from './CMSLayout'

const meta: Meta<typeof CMSLayout> = {
  component: CMSLayout,
}

export default meta

type Story = StoryObj<typeof CMSLayout>

export const Primary: Story = {}
